#include <iostream>
using namespace std;
int main() {
    //operator Aritmatika
    int a = 10, b = 20;
    cout << "operatorn Aritmatika:" << endl;
    cout << "a + b = " << a + b << endl; //penjumlahan
    cout << "a - b = " << a - b << endl; //pengurangan
    cout << "a * b = " << a * b << endl; //perkalian
    cout << "b / a = " << a / b << endl; //pembagian
    cout << "a % b = " << a % b << endl; //modulus

    // operator penugasan
    int c = 5;
    std::cout << "\nOperator Penugasan:" << std::endl;
    c += 3; // c = c + 3
    std::cout << "c += 3; c= " <<c<< std::endl;

    c -= 2; // c = c-2
    std::cout << "c = 2; = " << C << std::endl;
    // Operator Logika
    bool x true, y false;
    std::cout << "\nOperator Logika:" << std::endl;
    std::cout << "x && y: " << (x && y) << std::endl;
    std::cout << "x || y: "<< (x || y) << std::endl;
    std::cout << "!x: " << (1x) << std::endl;
